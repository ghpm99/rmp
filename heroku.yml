setup:
  addons:
    - plan: heroku-postgresql
      as: rmp-server
build:
  docker:
    web: Dockerfile
run:
  release:
    command:
      - python manage.py migrate
    image: web
  web: gunicorn rmp.wsgi